#include "main.h"

/**
 * swap_int - function that swaps the values of two integers.
 * @a: a pointer to the first integer
 * @b: a pointer to the first integer
 */

void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}

#include "main.h"

/**
 * add - function that adds two integers and returns the result
 *
 * @a: is a first integer
 * @b: is a second inteeger
 *
 * Return: the result of adds
 *
 */

int add(int a, int b)
{

	int resultat = a + b;

	return (resultat);
}
